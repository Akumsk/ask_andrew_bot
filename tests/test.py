import fitz
print(fitz.__file__)        # Should show the path to the module
print(fitz.__doc__)         # Should display module documentation
print(fitz.__version__)     # Should show the version number
print(dir(fitz))